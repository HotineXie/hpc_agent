# HPC Agent

HPC Agent is a local AI agent runner with HPC job submission, endpoint
management, and node-hour budget tracking.

## Setup

```bash
bash setup.sh
```

The setup is self-contained and does not require an external upstream checkout.
